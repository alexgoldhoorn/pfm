#!/bin/bash
# Portfolio Manager CLI wrapper script

# Change to the directory containing this script
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd "$SCRIPT_DIR"

# Run the portfolio manager with all passed arguments
python -m portf_manager "$@"
